// open pop up funtion 

function togleDesc() {
    // select the pop up to togle display block and none
    // const exploreBtn = document.querySelectorAll('.explore-btn')
    // query select the pop up
    const popUp = document.querySelector('#pop-up')

    popUp.style.display == 'none' ? popUp.style.display = 'block' : popUp.style.display = 'none' ;

    // popUp.style.display == 'none' ? popUp.style.display == 'block' : popUp.style.display == 'none' ;
 
}
